package com.example.androidstudiologinarayztasarm_150ders;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class MainActivity extends AppCompatActivity {
   /* Başlangıcta intertnetten bir gradient xml kodu kopyala
    daha sonra drawble klasöründe yeni bir tane gradient diye
    xml tanımla ve kodu orya yapoştır*/

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);
    }
}
